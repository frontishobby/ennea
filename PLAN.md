# ENNEA

마우스 기반 리듬게임. 3×3 그리드로 날아오는 노트를 커서로 받고, 클릭과 스크롤로 별도 레인을 처리한다.
서버 없이 정적 호스팅으로 동작하며, 1v1 대전은 P2P(WebRTC)로 붙는다.

이름 유래: 그리스어 9 (3×3 그리드).

---

## 1. 확정된 제약

| 항목 | 결정 |
|---|---|
| GitHub 계정 | **frontishobby 전용** (david02324 절대 사용 금지) |
| 해상도 | **1280×720 고정**, 바깥은 레터박스 |
| 프레임워크 | Svelte + TypeScript |
| 렌더러 | PixiJS |
| 호스팅 | GitHub Pages (정적) |
| 백엔드 | **없음.** 인증·DB·서버 로직 전부 없음 |
| PWA | 지원 (오프라인 플레이 + 에셋 영구 캐시) |
| 커밋 | AI author/co-author 트레일러 금지 |
| 곡 | AI 생성곡 또는 CC 라이선스 곡, 한정 수량을 저장소에 동봉 |

### 백엔드를 두지 않는 이유

이 프로젝트의 진짜 리스크는 **온셋 검출 품질과 오디오 캘리브레이션**이다. 백엔드는 그 리스크와 무관한데 시간을 먹는다.
글로벌 리더보드는 유저가 있어야 의미가 있고, 서버 랭킹이 없으면 **치팅 방어 로직 전체가 불필요**해진다.
사람이 모인 뒤에 붙인다. §14에 이전 비용을 0에 가깝게 유지하는 규칙을 둔다.

---

## 2. 기술 스택

- **Svelte + TypeScript** — UI 셸(메뉴, 곡 선택, 결과, 설정)
- **PixiJS** — 게임 캔버스. WebGL 스프라이트 배칭. Phaser는 물리·씬·타일맵 등 안 쓸 기능이 많아 과함
- **Vite** — 번들러
- **Trystero** — P2P 시그널링/데이터 채널
- **Web Audio API** — 재생 및 타이밍 기준 시계
- **Workbox 또는 수동 Service Worker** — PWA 캐싱
- **ffmpeg** (오프라인) — 음원 인코딩
- **Node 스크립트** (오프라인) — 채보 생성

---

## 3. 화면 / 해상도 정책

**논리 해상도 1280×720 고정.** 모든 좌표·속도·판정 거리는 이 좌표계에서만 계산한다.

- PixiJS 스테이지를 1280×720으로 고정 생성
- 뷰포트가 1280×720보다 **작을 때만** CSS `transform: scale()` 로 비율을 유지한 채 줄인다
- **1배를 넘겨 확대하지 않는다.** 화면이 아무리 커도 스테이지는 1280×720에서 멈추고
  남는 영역은 전부 레터박스다. 업스케일을 허용하면 기기마다 3×3 그리드의 물리적 크기가
  달라져서, 감도가 같아도 체감 난이도가 달라진다. 정수배가 아닌 확대는 그림도 뭉갠다
- 남는 영역은 레터박스(상하) / 필러박스(좌우), 배경은 단색
- **Pointer Lock을 쓰므로 실제 커서는 가상이다.** `movementX/Y`를 논리 좌표에 누적하고 1280×720으로 클램프한다
  - → 표시 배율이 감도에 영향을 주지 않는다. 어떤 화면 크기에서도 감도가 동일하다
  - → 스케일 보정 계산이 불필요하다. 이 설계의 핵심 이점

```
┌─────────────────────────────┐
│ ███ 레터박스 ███████████████ │
│ ┌─────────────────────────┐ │
│ │      1280 × 720         │ │
│ │  모든 게임 로직의 좌표계  │ │
│ └─────────────────────────┘ │
│ ███ 레터박스 ███████████████ │
└─────────────────────────────┘
```

### 레이아웃

위치 무관 노트(클릭·스크롤)를 3×3 안에 그리면 "저기로 가야 하나?" 혼동이 생긴다.
사각형 **바깥**에서 처리한다.

화면 가운데에 큰 정사각형 하나(360×360). **네 변이 곧 판정선이다.**
이산 노트는 바깥에서 안으로 밀려와 **앞면이 변에 닿는 순간**이 판정 시점 —
선과 선이 만나는 순간이라 눈으로 읽기 쉽다.

```
              스크롤 ↑
         ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓
        ╭───────────────╮
   좌   │               │   우
  클릭 →│   커서 영역    │← 클릭
        │               │
        ╰───────────────╯
         ↑ ↑ ↑ ↑ ↑ ↑ ↑ ↑
              스크롤 ↓
```

**안쪽 3×3 격자는 그리지 않는다.** 커서 노트가 뜰 때 그 자리만 줄어드는 사각형으로
보여주면 되고, 격자를 상시로 그리면 노트를 가린다. 커서 노트의 접근 사각형은 구역보다
커서 경계를 넘으므로 사각형으로 마스킹한다 — 안쪽 일은 안쪽에서만 보인다.

리드 타임은 이산 노트 900ms 로 통일한다. 변까지 거리가 달라(좌우 460px, 위아래 180px)
속도는 달라지지만 반응 시간이 같은 쪽이 플레이에 중요하다. 커서는 600ms — 연속 입력이라
매 노트에 반응하는 게 아니라 궤적을 따라가는 것이고, 구역이 붙어 있어 길게 잡으면 서로 덮는다.

---

## 4. 입력 설계

### 노트 타입

| 타입 | 입력 | 판정 |
|---|---|---|
| `cursor` | 마우스 이동 | 판정 시점에 해당 칸 안에 커서가 있으면 성공 (위치 판정) |
| `click` | 좌/우 클릭 | 타이밍 판정. 커서 위치 무관 |
| `scroll` | 휠 ↑/↓ | 타이밍 판정. 커서 위치 무관 |

커서 노트는 **연속 입력**, 클릭·스크롤은 **이산 입력**. 두 레이어가 독립적으로 돌아가면서 동시 수행 난이도를 만든다.

### Pointer Lock

거의 필수다. 커서가 화면 밖으로 안 나가고, 컨텍스트 메뉴가 안 뜨고, 상대 좌표로 감도 조절이 된다.

- 진입에 유저 제스처 필요 → "클릭해서 시작"
- ESC로 풀리면 **자동 일시정지**
- `contextmenu` `preventDefault()` 필수 (macOS는 Ctrl+클릭도 우클릭으로 들어옴)
- **마우스 감도 설정이 필수 기능**이 된다. 이 장르 유저는 감도에 극도로 민감하다

### 스크롤 정규화 — 가장 위험한 부분

`wheel` 이벤트는 디바이스마다 의미가 완전히 다르다.

- 일반 휠: 노치마다 한 번, `deltaY` ±100 (OS·브라우저별로 ±120)
- 트랙패드: 관성 스크롤로 **한 번 튕기면 수십~수백 이벤트**가 `deltaY` 1~5씩
- 프리스핀 휠: 연속 수백 개
- `deltaMode`도 PIXEL / LINE / PAGE로 갈림

그대로 두면 트랙패드 유저가 한 번에 노트 10개를 쓸어담는다. 공정성이 근본적으로 깨진다.

```ts
let accum = 0, lastDir = 0, cooldownUntil = 0
const TICK = 50        // 누적 임계값 (deltaMode 정규화 후)
const COOLDOWN = 70    // 틱 후 무시 구간(ms) — 관성 연타 차단

canvas.addEventListener('wheel', (e) => {
  e.preventDefault()
  const dy = e.deltaMode === 1 ? e.deltaY * 16      // LINE
           : e.deltaMode === 2 ? e.deltaY * 400     // PAGE
           : e.deltaY                                // PIXEL
  const dir = Math.sign(dy)
  if (dir !== lastDir) { accum = 0; lastDir = dir }  // 방향 전환 시 리셋
  accum += Math.abs(dy)

  if (accum >= TICK && e.timeStamp >= cooldownUntil) {
    emitScrollTick(dir, e.timeStamp)   // 타임스탬프는 이벤트 것을 그대로
    accum = 0
    cooldownUntil = e.timeStamp + COOLDOWN
  }
}, { passive: false })
```

`passive: false` + `preventDefault()` 없으면 페이지가 같이 스크롤된다. 캔버스에 `overscroll-behavior: none`도 건다.

이래도 완전히 공평해지지 않으므로 **채보 생성기에 "스크롤 노트 최소 간격 200ms" 제약**을 건다. 스크롤 연타 구간은 만들지 않는다.

### 키보드 폴백 (필수)

트랙패드에서 스크롤 노트는 관성 때문에 사실상 플레이 불가능하다. 노트북 유저가 통째로 떨어져 나간다.

- 스크롤 ↑/↓ → `W` / `S`
- 좌/우 클릭 → `Z` / `X`

데스크톱 전용이라 이미 좁은 판인데 여기서 더 깎을 이유가 없다.

### 입력 타임스탬프

- **`mousedown` / `pointerdown`을 쓰고 `click`은 쓰지 않는다.** `click`은 down+up 후 발생해서 지연이 얹힌다
- **이벤트 핸들러에서 `event.timeStamp`를 그대로 캡처해 큐에 넣고, rAF에서는 소비만 한다.** rAF 시각으로 판정하면 최대 16ms 오차가 붙는데, 리듬게임에서 16ms면 판정 등급이 바뀐다

---

## 5. 오디오 & 타이밍

### 시계 분리 (핵심 원칙)

- **판정은 `AudioContext.currentTime` 기준**
- **렌더는 rAF**
- 둘을 섞지 않는다. rAF 시각으로 판정하면 프레임이 튀는 순간 판정이 같이 틀어진다 — 이 장르에서 원인 찾기 제일 어려운 버그

```ts
const audioStart = ctx.currentTime
const perfStart  = performance.now()
const songTimeOf = (evTimeStamp: number) =>
  (evTimeStamp - perfStart) / 1000 - userOffsetSec
```

### 재생 방식

`<audio>` 스트리밍이 아니라 **fetch → ArrayBuffer → `decodeAudioData` → `AudioBufferSourceNode`**.
정확한 시킹과 타이밍이 필요하다. 그래서 파일 크기가 로딩 시간에 직결된다.

### 캘리브레이션 (필수 기능)

렌더링 라이브러리가 해결해주지 않는다. 별도 화면이 필요하다.

- 유저가 비트에 맞춰 입력 → 오프셋 측정 → localStorage 저장
- 블루투스 이어폰은 100~300ms가 밀린다. 이게 없으면 "게임이 이상하다"는 피드백만 받고 원인을 못 찾는다
- `AudioContext.outputLatency` / `baseLatency`는 참고값으로만. 부정확하다

### 인코더 딜레이 (priming samples)

MP3·AAC는 디코딩 출력 맨 앞에 무음이 붙는다. MP3 ~1100샘플(~25ms), AAC ~2100샘플(~48ms).
갭리스 메타데이터(LAME 태그, iTunSMPB)를 `decodeAudioData`가 처리하는 방식이 **브라우저마다 다르다**.

→ 같은 파일인데 Chrome과 Safari에서 채보가 수십 ms 어긋난다. 재현 안 되는 버그로 나타난다.

**Opus는 헤더에 pre-skip이 명시돼 있고 디코더들이 일관되게 처리한다.** 용량뿐 아니라 이 이유로도 Opus를 쓴다.
그래도 방심하지 말고 채보 메타의 `audioOffsetMs`를 곡마다 실측해서 박아둔다.

### 인코딩

```bash
# 주력
ffmpeg -i source.wav -c:a libopus -b:a 128k -vbr on -application audio -ac 2 out.webm

# Safari 폴백이 필요하면
ffmpeg -i source.wav -c:a aac -b:a 160k out.m4a
```

- `-application audio` 명시 (libopus가 음성 모드로 튈 수 있음)
- 128k 기본, 곡이 많아지면 96k로 내려도 게임 중엔 차이를 못 느낌
- 3분 곡 ≈ 2.7MB
- **Safari에서 `decodeAudioData`까지 반드시 실테스트.** `<audio>`로는 되는데 디코드에서 문제가 생기는 경우가 따로 있다

### 소스 음원은 무손실로 확보

AI 생성곡이 mp3로만 나오면 **손실 → 손실 재인코딩**이 되어 고역이 뭉개진다.
하필 그 대역(하이햇·심벌)을 온셋 검출에 쓰므로 **채보 품질에 직접 영향**이 간다.

→ WAV/FLAC으로 받아서 **그 원본으로 채보를 생성**하고, 배포용 Opus는 따로 뽑는다. 분석 소스와 배포 소스를 분리한다.

---

## 6. 채보 생성 파이프라인 (오프라인 Node 스크립트)

### 런타임 생성을 하지 않는 이유

"결정적 알고리즘"의 목적은 **재생성 가능성**이지 클라에서 매번 돌리라는 게 아니다.

- 3분 곡 디코드 + FFT = 곡 진입마다 수 초
- **브라우저·CPU 간 부동소수점 차이로 채보가 미세하게 달라질 수 있다.** 1v1 공정성이 깨지고 재현이 안 되니 디버깅도 불가능

→ 오프라인에서 한 번 생성 → 저장소에 커밋 → 클라는 받기만.

### 파이프라인

**① STFT** — 프레임 2048, hop 512 (44.1kHz에서 11.6ms 해상도). Hann 윈도우.
- 주의: 2048 FFT면 빈 하나가 21.5Hz. 20~150Hz 저역에는 빈이 7개뿐이라 킥 검출 해상도가 낮다.
  잘 안 잡히면 프레임 4096으로 올리거나 저역만 다운샘플링 후 별도 FFT.

**② 로그 압축** — `log(1 + 1000·|X|)`. **빼먹으면 조용한 인트로·브릿지에서 노트가 안 나온다.** 작아 보이지만 결과가 갈린다.

**③ Spectral Flux** — 대역별로 프레임 간 **증가분만** 합산. 감쇠는 온셋이 아니다.
```
SF[n] = Σ_k max(0, |X[n,k]| - |X[n-1,k]|)
```

**④ 적응형 임계값** — 고정 임계값은 절대 금지.
```
thr[n] = δ + α · median(SF[n-w : n+w])     // w ≈ ±0.3초
```
조용한 벌스와 터지는 후렴이 한 곡에 있어도 둘 다 잡힌다.

**⑤ 피크 피킹** — 임계값 초과 + 국소 최대 + 최소 간격 30ms.

**⑥ BPM 추정 + 그리드 스냅** — 품질을 제일 크게 올리는 단계.
- IOI 히스토그램 또는 자기상관으로 BPM 추정
- 각 온셋을 가장 가까운 16분음표에 스냅. 검출이 ±20ms 흔들려도 그리드에 붙으면 리듬이 깔끔해진다
- 스냅 거리가 임계값보다 멀면 스냅하지 않고 그대로 (셋잇단·스윙 보존)

**⑦ 대역 → 타입 배정**

온셋 검출은 "언제"만 주지 "무엇을"은 안 준다. 대역으로 가르면 음악적으로 말이 되는 배치가 나온다.

| 대역 | 악기 | 노트 타입 | 근거 |
|---|---|---|---|
| 저역 (20~250Hz) | 킥·베이스 | `scroll` | 묵직한 액션, 박자가 성김 → 최소 간격 제약과 맞음 |
| 중역 (250~2kHz) | 스네어·보컬 | `cursor` | 곡의 주 리듬이 커서 이동이 되는 게 자연스러움 |
| 고역 (4k~16kHz) | 하이햇·심벌 | `click` | 빠른 연타가 가능한 유일한 입력 |

**⑧ 커서 배치** — §7 참조

**⑨ 제약 검사** — 위반 시 패턴 교체 재시도

### 소스 분리는 나중 옵션

지금 하는 건 **소스 분리가 아니라 대역별 온셋 검출**이다. 킥 트랙을 뽑아내는 게 아니라 "저역 에너지가 급증한 순간"을 찾는 것이고, 실제 음악에서 대부분 맞아떨어진다.

진짜 분리(Demucs 등)는 오프라인이므로 파이프라인에 끼울 수 있다. 곡당 1분씩 돌려도 수십 곡이면 현실적이다. **품질을 확 올리는 후순위 카드로 기억해 둔다.**

### 현실적 기대치

드럼이 명확한 EDM·팝은 잘 나온다. 어쿠스틱·재즈·발라드는 별로다.

~~AI 생성 EDM은 비트가 기계적으로 정확해서 검출률이 높다.~~ **틀렸다 — 첫 실제 곡에서 깨졌다.**
ACE-Step(디퓨전) 출력은 메트로놈이 없다. future-core 128초에서 BPM 이 ±1.4~1.9% 흔들렸고
(8초 창 기준 143.6~149.2), 프롬프트의 160 BPM 은 지켜지지 않았다(실제 145.8). "four on the floor kick"
을 써도 저역이 킥 없는 베이스 벽으로 나올 수 있다. 브레이크다운은 셋잇단으로 돌았다.
→ 전역 고정 그리드는 정렬률이 확률 수준(36% vs 34%)이라 못 쓴다. **지역 비트 그리드**(8초 창,
브루트포스 위상, 귀무 기준선 대비 신뢰도)와 **대역·구간별 적합도 게이트**가 필요했다.
자세한 건 `tools/chartgen/README.md`. 음악 쪽에서는 프롬프트에 드럼 키트를 명시적으로 시키고
(`drum-forward` 프리셋), 뽑은 뒤 생성기의 대역 적합도 세 줄로 곡을 고른다.

자동 생성은 **초안**으로 보고, **노트 추가/삭제/타입변경/시간 미세조정만 되는 간단한 보정 툴**을 만든다.
에디터 전체가 아니라 그 네 가지면 충분하다. 곡이 수십 개면 곡당 20분 다듬는 게 알고리즘을 몇 주 튜닝하는 것보다 품질이 높다.

---

## 7. 3×3 커서 배치 — 재미를 결정하는 부분

**핵심: 위치가 9개뿐이라 "위치"로는 다양성이 안 나온다. 다양성은 전이(transition)에서 나온다.**
9개 점 사이 전이는 72가지, 여기에 타이밍이 곱해진다. **노트를 하나씩 배치하지 말고 패턴 단위(2~8개 묶음)로 배치한다.**

### ① 이동 거리 예산 (가장 먼저 거는 제약)

두 노트 사이 시간 Δt, 그리드 거리 d에 대해 `d / Δt ≤ v_max(난이도)`.
없으면 인간이 못 치는 채보가 나온다.

**거리는 유클리드가 아니라 체비셰프(대각선 = 1)** 를 쓴다. 실제 마우스는 대각선을 한 번에 긋지 가로세로로 꺾지 않는다.

### ② 패턴 어휘

순수 알고리즘보다 손으로 정의한 템플릿 세트가 훨씬 재밌는 채보를 낸다.

| 패턴 | 궤적 | 어울리는 구간 |
|---|---|---|
| 스트림 | 인접 칸 연속 (좌상→상→우상→우) | 빠른 하이햇, 필 |
| 점프 | 반대편으로 크게 (좌상→우하) | 강한 킥, 드롭 |
| 핑퐁 | 두 점 왕복 (좌↔우) | 반복 리프 |
| 오빗 | 외곽 8칸 원형 순회 | 지속음, 빌드업 |
| 센터 앵커 | 중앙↔외곽 반복 | 콜앤리스폰스 |
| 지그재그 | 행 사이 번갈아 | 스네어 연타 |
| 스테이 | 같은 칸 유지 | 쉬는 구간 |

### ③ D4 대칭으로 변형 8배

3×3는 정사각형이라 **회전 4 × 반사 2 = 8개 변환**이 공짜다. 패턴 20개 → 160가지.
"다채로움"을 얻는 가장 값싼 방법이고, 변환은 좌표 스왑 몇 줄이다.

### ④ 음악 구조에 매핑

**같은 멜로디 구간에는 같은 패턴을 재사용한다.** 리듬게임의 쾌감 상당 부분이 "아까 그거다" 하는 인지에서 온다. 매번 새로운 배치는 다채로운 게 아니라 지루하다.

- 섹션 반복 검출: 크로마그램 자기유사 행렬이 정석. 간단히는 온셋 패턴 자기상관으로 마디 반복
- 후렴(고에너지)엔 큰 점프 + 높은 밀도, 벌스엔 스트림 + 낮은 밀도
- 마디 첫 박에는 중앙이나 코너 같은 강한 위치를 앵커로

### ⑤ 관성 규칙

직전 이동 방향을 기억해 같은 방향 계열에 가산점. 매 노트마다 꺾이면 손목이 아프다.
급반전은 임팩트가 필요한 순간에만.

### ⑥ 타입 간 상호작용 제약

- **커서 노트 판정 시점 ±50ms 안에 스크롤 노트 금지.** 휠을 굴리면 마우스가 물리적으로 흔들려 커서 노트를 놓친다. 소프트웨어로 못 고치는 문제
- 클릭은 덜 흔들리니 겹쳐도 됨. 최상위 난이도에서만 의도적으로 겹침
- **스크롤 노트 직후 커서를 먼 거리로 보내지 않는다.** 손이 휠에 가 있어 반응이 느리다

### ⑦ 결정성

패턴 선택 PRNG 시드를 `hash(songHash + difficulty + generatorVersion)`에서 유도.
같은 입력이 항상 같은 채보를 낸다. `chartHash` 체계와 맞물린다.

---

## 8. 에셋 구조 & 채보 포맷

```
static/
  songs.json                    # 전곡 메타데이터 (나중에 DB 테이블이 될 형태)
  songs/<slug>/audio.webm
  songs/<slug>/audio.m4a        # Safari 폴백이 필요하면
  songs/<slug>/cover.webp
  charts/<chartHash>.json
```

### songs.json

```json
{
  "version": 1,
  "songs": [
    {
      "slug": "neon-drift",
      "title": "Neon Drift",
      "artist": "...",
      "license": "CC-BY-4.0",
      "sourceUrl": "...",
      "durationMs": 183000,
      "bpm": 128,
      "previewStartMs": 42000,
      "audioOffsetMs": 0,
      "charts": [
        { "difficulty": "normal", "chartHash": "a1b2c3...", "noteCount": 412 },
        { "difficulty": "hard",   "chartHash": "d4e5f6...", "noteCount": 789 }
      ]
    }
  ]
}
```

### charts/<chartHash>.json

```json
{
  "version": 1,
  "generator": "onset-v1",
  "difficulty": "hard",
  "audioOffsetMs": 0,
  "sections": [0, 16000, 32000, 48000],
  "notes": [
    { "t": 1250, "type": "cursor", "x": 1, "y": 2 },
    { "t": 1500, "type": "click",  "btn": "L" },
    { "t": 1750, "type": "scroll", "dir": "up" }
  ]
}
```

- **`t`는 정수 ms로 고정.** 부동소수점을 넣으면 직렬화 방식에 따라 해시가 흔들려 무결성이 무너진다
- `sections`는 §10 구간 승부의 경계. 채보 생성 시 섹션 검출 결과를 그대로 재사용

### chartHash를 파일명으로 쓰는 이유

내용이 바뀌면 이름이 바뀌니 영구 캐시가 안전하고, 덤으로 세 가지가 따라온다.

1. **1v1 무결성** — 매칭 시 `chartHash` 한 줄 비교로 양쪽이 같은 채보인지 확정. 생성기 버전이 엇갈린 채 점수를 비교하는 사고를 원천 차단
2. **리플레이 영속성** — 리플레이에 `chartHash`를 박아두면 몇 달 뒤에도 정확히 그 채보로 재생
3. **기록 무결성** — 기록은 `songId`가 아니라 **`chartHash` 단위**로 매단다. 알고리즘을 개선하면 채보가 달라지는데, 곡 단위로 쌓았으면 그 순간 과거 기록이 전부 오염된다. 새 채보 = 새 기록 슬롯, 구 채보는 비활성 표시하되 데이터는 남겨 옛 기록과 리플레이를 유효하게 둔다

### 경로 상수

```ts
export const AUDIO_BASE_URL = import.meta.env.BASE_URL + 'songs/'
export const CHART_BASE_URL = import.meta.env.BASE_URL + 'charts/'
```

나중에 R2나 Supabase Storage로 옮길 때 이 두 줄만 바꾸면 된다.

---

## 9. 싱글 플레이

서버가 없어도 다음이 전부 성립한다.

- **프리 플레이** — 곡 선택 후 플레이. 개인 기록은 localStorage
- **데일리 챌린지** — `hash(오늘 날짜)`로 곡과 난이도를 결정. 전 세계가 같은 판을 푼다. Wordle 방식. 서버 불필요
- **고스트 대전** — 저장된 리플레이를 상대 슬롯에 재생
  - 내 최고 기록 (localStorage)
  - **레퍼런스 고스트** — 개발자가 직접 친 기록을 정적 자산으로 번들. 혼자서도 상대가 생긴다
  - 친구 기록 — 리플레이 파일 교환 또는 P2P 전송
  - **멀티의 상대 표시 코드와 완전히 같은 경로를 쓴다.** 실시간 스트림을 꽂으면 멀티, 저장된 리플레이를 꽂으면 싱글 고스트

### 리플레이 포맷 (지금 확정할 것)

나중에 포맷이 바뀌면 그동안 쌓인 기록이 전부 날아간다.

```ts
interface Replay {
  version: 1
  chartHash: string
  generator: string
  createdAt: number        // epoch ms
  audioOffsetMs: number
  userOffsetMs: number     // 플레이어 캘리브레이션 값
  cursor: Int16Array       // [t, x, y] × N, 20~60Hz 샘플
  discrete: Int32Array     // [t, typeCode] × N — 클릭·스크롤
  result: { score: number; combo: number; accuracy: number; judgements: number[] }
}
```

커서는 20~60Hz 샘플링 + 델타 인코딩으로 한 판 수십 KB. 이산 입력은 훨씬 작다.

---

## 10. 멀티 플레이 (1v1, P2P)

### 왜 총점 비교가 아닌가

**총점 대결은 역전이 안 된다.** 실력차가 있으면 30초에 승부가 결정되고 남은 2분이 양쪽 다 무의미해진다.
지는 쪽은 이탈하고 이기는 쪽도 재미없다. 리듬게임 1v1의 근본 과제.

### 구간 승부제

곡을 `sections` 경계로 쪼개고 **구간마다 정확도 높은 쪽이 1점.** 이긴 구간 수로 승부. 테니스 구조.

- 한 구간을 망쳐도 손실이 그 구간 1점으로 **격리**된다. 다음 구간은 0-0에서 다시 시작
- **후렴/마지막 구간에 2점 가중치**를 주면 끝까지 붙는다
- 동기화 부담 0 — 구간 끝에 결과 한 줄씩만 교환
- 옵션: 뒤처진 쪽 판정 창을 살짝 넓히는 러버밴딩. 캐주얼에만 켜고 랭크에선 끔

### 상대 표현

화면 분할은 원근 뷰라 불가능. **내 화면에 상대 커서를 반투명 다른 색으로 겹친다.**
상대가 어디를 치는지 보이는 것만으로 압박감이 크고, 잘하는 사람의 궤적을 보며 배운다.

- **10~20Hz로 보내고 클라에서 보간.** 초당 20회 × 좌표 2개 ≈ 80B/s
- 싱글 고스트와 같은 렌더링·보간 경로를 쓴다

### 간섭 요소

연속 구간 승리 등의 조건으로 상대에게 방해를 건다. 화면 흐림, 노트 페이드아웃, 그리드 미세 회전, 가짜 노트.

**상대의 채보 자체는 절대 바꾸지 않는다.** 노트가 추가·삭제되면 `chartHash` 무결성이 깨지고 리플레이 재생과 기록 비교가 전부 무너진다. **시각 효과만.** 판정해야 할 노트는 양쪽이 끝까지 동일하다.

### 동기화 실무

- **시작 타이밍은 엄밀히 맞출 필요 없다.** 각자 자기 오디오 클럭으로 플레이하고 결과만 교환한다. 3-2-1 카운트다운 후 각자 시작. 수십 ms 차이는 구간 승부에서 무의미
- 커서 고스트 어긋남이 거슬리면 WebRTC 왕복시간으로 클럭 오프셋을 3~5회 측정해 보정
- 브로드캐스트 빈도: 커서 20Hz / 점수·콤보 2~4Hz / 구간 결과 구간당 1회
- **연결이 끊겨도 게임이 안 깨진다.** 각자 로컬에서 완결되므로 "상대 연결 끊김 — 계속할까요?"를 띄우고 기록은 남긴다. P2P 리듬게임의 진짜 장점

### 매칭

Supabase Realtime Presence가 없으므로 Trystero 공개 릴레이(BitTorrent 트래커·Nostr·MQTT)만 남는다.
**이게 Trystero 기반 프로젝트의 가장 흔한 실패 지점이다.** 트래커가 죽거나 막히면 매칭이 안 된다.

- **친구 초대 링크가 1순위.** `#room=xxx` 해시로 직접 연결. 둘 다 동시에 들어와 있어 성공률이 랜덤 매칭보다 훨씬 높다
- **랜덤 매칭은 "되면 좋은 것"으로 강등.** 5~10초 안에 안 잡히면 고스트 대전으로 넘긴다
- 나중에 사람이 모이면 Trystero 전략만 갈아끼운다

### 치팅

**서버 랭킹이 없으므로 조작 동기 자체가 없다.** 실시간 판정에서 상대 점수를 그냥 믿는다.
검증 로직(재시뮬레이션, 커밋-리빌, 이상 탐지)은 **전부 만들지 않는다.** 백엔드를 붙이는 시점에 재검토.

---

## 11. PWA / 캐싱

**GitHub Pages는 `Cache-Control`을 제어할 수 없다.** 짧은 `max-age`가 고정으로 붙어서 `immutable` 영구 캐시를 걸 방법이 없다.
곡 진입마다 재검증 왕복(304)이 끼는데 리듬게임에서 거슬린다.

**Service Worker로 직접 캐시하면 해결된다.**

- 곡·채보는 해시 파일명이라 불변 → `cache-first`로 한 번 받으면 영구 보관
- 앱 셸은 `stale-while-revalidate`
- 오프라인 플레이가 공짜로 따라온다
- 설치 프롬프트, 전체화면 모드 (`display: fullscreen`)

곡을 IndexedDB나 Cache Storage에 디코드 전 ArrayBuffer로 두면 재방문 시 즉시 시작된다.

---

## 12. 저장소 & 배포

- **GitHub Pages 사이트 용량 상한 1GB.** Opus 128k 3분 곡 ≈ 2.7MB → 이론상 370곡. 30~50곡이면 100~150MB로 여유
- **대역폭 소프트 리밋 월 100GB** → 곡 다운로드 약 37,000회. SW 캐시 감안하면 실질은 훨씬 큼
- **`.nojekyll` 필수** (없으면 `_`로 시작하는 파일이 무시됨)
- 저장소는 **public** (private Pages는 유료)
- **음원 바이너리를 자주 갈아끼우면 git 히스토리에 영구히 쌓여 clone이 무거워진다.** 음원은 한 번 넣고 안 바꾸거나 별도 저장소로 분리
- 배포: GitHub Actions → `gh-pages` 또는 Pages 네이티브 빌드

대역폭이 실제로 문제가 됐을 때 jsDelivr(`cdn.jsdelivr.net/gh/...`, 태그·커밋 고정 시 장기 캐시)나 Cloudflare R2를 검토.
**지금 결정할 필요 없다.** `AUDIO_BASE_URL` 한 줄이다.

### 계정

```bash
git config user.name  "frontishobby"
git config user.email "92085928+frontishobby@users.noreply.github.com"
```

로컬 설정으로 고정되어 있다. 전역 설정(david02324)이 새지 않는다.
푸시 시 `gh auth switch --user frontishobby` 또는 해당 계정 원격 사용.

**커밋 메시지에 AI author / Co-Authored-By 트레일러를 넣지 않는다.**

---

## 13. 마일스톤

승부는 게임 로직이 아니라 **온셋 검출 품질과 캘리브레이션**에서 난다. 거기를 먼저 친다.

### M0 — 스캐폴딩
Svelte + TS + Vite + PixiJS, 1280×720 레터박스 셸, 라우팅, 빈 씬

### M1 — 타이밍 코어 (가장 중요)
- `AudioContext` 기반 시계, 판정/렌더 시계 분리
- 캘리브레이션 화면
- 수동 작성한 테스트 채보 하나로 판정 검증
- **여기서 "노트가 음악에 맞게 떨어지는가"를 귀로 확인한다. 이게 안 되면 나머지가 무의미**

### M2 — 입력
Pointer Lock, 커서 논리좌표, 스크롤 정규화, 클릭, 키보드 폴백, 감도 설정

### M3 — 채보 생성기 v1 (오프라인)
STFT → 로그 압축 → spectral flux → 적응형 임계값 → 피크 피킹 → BPM 스냅 → 대역별 타입 배정
**커서는 일단 랜덤 배치.** ①②까지의 결과를 귀로 검증하는 게 목적

### M4 — 커서 배치기
거리 예산, 패턴 어휘, D4 변형, 섹션 매핑, 관성, 타입 간 제약

### M5 — 싱글 완성
곡 선택, 결과 화면, localStorage 기록, 리플레이 저장/재생, 고스트, 데일리 챌린지

### M6 — PWA
Service Worker, 오프라인, 설치

### M7 — 1v1
Trystero, 친구 초대 링크, 구간 승부, 상대 커서 고스트, 연결 끊김 처리

### M8 — 콘텐츠
곡 확보(무손실), 인코딩, 채보 생성 + 수동 보정, 보정 툴

---

## 14. 나중에 백엔드를 붙일 때를 위한 원칙

이 다섯 가지만 지키면 이전 비용이 거의 0이다.

1. **메타데이터를 `songs.json` 하나로 분리.** 코드에 하드코딩 금지. 그대로 DB 테이블이 된다
2. **경로를 상수로.** `AUDIO_BASE_URL`, `CHART_BASE_URL` 두 줄로 스토리지 이전
3. **`chartHash` 파일명 유지.** 무결성·캐시·1v1 채보 일치 확인이 전부 여기 걸려 있고 백엔드와 무관
4. **리플레이 포맷을 지금 확정.** 나중에 바뀌면 그동안의 기록이 날아간다
5. **localStorage 스키마에 버전 필드.** 서버 마이그레이션 때 필요

붙인다면 순서는 — Supabase OAuth + `profiles`(닉네임·캘리브레이션·감도 동기화) → `chartHash` 단위 리더보드 → Realtime Presence 로비 → 리플레이 Storage → (랭크를 열 때만) Edge Function 검증.

---

## 15. 미해결 / 검증 필요

- [ ] **Safari `decodeAudioData`에서 Opus/WebM 실동작 확인** (macOS + iOS). 실패 시 m4a 폴백 + 포맷별 `audioOffsetMs` 실측
- [ ] 저역 온셋 검출 해상도 — 2048 FFT로 킥이 잡히는지, 안 되면 4096 또는 저역 별도 처리
- [ ] 온셋 검출 파라미터(δ, α, 윈도우 w, 최소 간격) 곡별 튜닝 범위 — 실제 곡 1곡으로만 봤다.
      추가로 필요했던 것: 감쇠 마스크(τ 80ms), 지배 대역 해소(±25ms), lag-3 flux. `tools/chartgen/README.md`
- [x] ~~AI 생성곡의 템포 안정성~~ — 불안정하다. ±1.4~1.9% 드리프트, 프롬프트 BPM 미준수, 셋잇단 브레이크다운.
      지역 비트 그리드로 해결. 변박·급격한 템포 변화는 아직 못 따라간다
- [ ] 셋잇단 구간의 신뢰도 여유(21~30%)가 정박(36~53%)보다 낮다 — 귀로 검증 중. 스윙 격자는 별도로 안 뒀다
- [ ] 스크롤 `TICK` / `COOLDOWN` 실측값 — 일반 휠 / 트랙패드 / 프리스핀 휠 3종에서
- [ ] 난이도별 `v_max`(거리 예산) 실측
- [ ] 곡 확보 경로 확정 — AI 생성 서비스의 무손실 출력 가능 여부, CC 곡 출처와 표기 의무
- [ ] `ennea` 도메인 / GitHub 저장소명 중복 확인
